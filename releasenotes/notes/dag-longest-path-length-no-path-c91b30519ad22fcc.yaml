---
features:
  - |
    Added a new rustworkx-core function ``longest_path_length`` to the module ``dag_algo``. This
    function computes the length of the longest path in a directed acyclic graph without storing
    the actual path or returning it. This differs from the existing ``longest_path`` which
    internally stores the path and returns it.
