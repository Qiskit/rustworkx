features:
  - |
    New functions `generate_random_path` in `rustworkx-core`. New functions
    `generate_random_path_digraph` and `generate_random_path_graph` in
    `rustworkx`. These functions return the nodes visited during a random walk
    on the graph.
