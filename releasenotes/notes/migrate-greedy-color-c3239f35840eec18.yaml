---
features:
  - |
    The function ``greedy_color``has been migrated to the ``rustworkx-core``
    crate in the ``coloring`` module. It vertex-color a graph using the 
    "largest first" greedy strategy.
