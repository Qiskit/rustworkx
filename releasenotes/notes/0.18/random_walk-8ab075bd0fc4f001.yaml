features_core:
  - |
    Added a new function ``generate_random_path`` to the
    ``rustworkx-core::traversal`` module for returning the path
    of nodes visited during a random walk on a graph.
features:
  - |
    Added a new new function :func:`.generate_random_path` to return
    return the path of nodes visited during a random walk on the graph.
