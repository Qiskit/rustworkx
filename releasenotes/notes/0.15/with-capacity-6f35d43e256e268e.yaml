---
features:
  - |
    :class:`.PyGraph` and :class:`.PyDiGraph` now each take keyword arguments ``node_count_hint``
    and ``edge_count_hint`` in their constructors, which can be used to pre-allocate space for
    the given number of nodes and edges.
